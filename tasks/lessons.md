## Typography Corrections

- When adjusting the home page, preserve the established heading font feel unless the user explicitly asks for a new visual direction.
- Avoid pushing landing page headings to ultra-heavy weights such as `900` or `950`; verify hero and section headers stay readable, balanced, and do not wrap unexpectedly on desktop.
